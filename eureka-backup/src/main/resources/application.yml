spring:
  application:
    name: eureka-server
  security:
    user:
      name: admin
      password: admin

server:
  port: 7001

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:7000/eureka/
    fetch-registry: false
    register-with-eureka: false